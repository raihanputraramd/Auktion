<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.auktion">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".adminActivity.AdminPendataanBarangActivity" />
        <activity android:name=".adminActivity.AdminDetailItemActivity" />
        <activity android:name=".adminActivity.AdminProfileActivity" />
        <activity android:name=".adminActivity.AdminMenuActivity" />
        <activity android:name=".userActivity.UserProfileActivity" />
        <activity android:name=".userActivity.UserDetailItemActivity" />
        <activity android:name=".userActivity.UserMenuActivity" />
        <activity android:name=".RegisterOrLoginActivity" />
        <activity android:name=".adminActivity.AdminRegisterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".userActivity.UserRegisterActivity" />
        <activity android:name=".AppIntroActivity" />

        <activity android:name=".LoginActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>